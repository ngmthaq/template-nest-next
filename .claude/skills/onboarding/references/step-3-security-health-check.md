# Step 3 — Security Health Check

This step is optional.

Ask the user:

> "Do you want to run a security health check? It looks for hardcoded secrets and known security issues. (yes/no)"

If the user approves:

- Load and run the [security-scanner](../../security-scanner/SKILL.md) skill.
