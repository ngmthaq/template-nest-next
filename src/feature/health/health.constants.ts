/** DI token for the shared MySQL connection pool used by health checks. */
export const HEALTH_MYSQL_POOL = 'HEALTH_MYSQL_POOL';

/** DI token for the shared Redis client used by health checks. */
export const HEALTH_REDIS_CLIENT = 'HEALTH_REDIS_CLIENT';
